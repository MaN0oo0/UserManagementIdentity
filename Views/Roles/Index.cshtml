
@{
    ViewData["Title"] = "Index";
}
@model IEnumerable<IdentityRole>

<div class="col-md-12">
    <h1>Roles</h1>



    <partial name="_RoleForm" model="new RoleFormViewModel()" />

    <div class="overflow-auto" style="max-height:300px !important">
        <table class="table table-striped mt-4 overflow-scroll">
            <thead>
                <tr class="bg-primary text-white">
                    <td>Id</td>
                    <td>Role Name</td>
                </tr>
            </thead>
            <tbody>
                @{
                    if (ViewBag.SelectList != null)
                    {


                        foreach (var item in (IEnumerable<SelectListItem>)ViewBag.SelectList)
                        {
                            <tr>
                                <td>@item.Value</td>
                                <td>@item.Text</td>
                            </tr>
                        }
                    }
                    else
                    {
                        foreach (var role in Model)
                        {
                            <tr>
                                <td>@role.Id</td>
                                <td>@role.Name</td>
                            </tr>
                        }
                    }
                }
            </tbody>
        </table>

    </div>

    @*<div class="row">
            <div class="col-md-8">
                <select class="form-select" asp-for="Id">
                    <option value="" selected>Chose Role</option>
                    @foreach (var role in (IEnumerable<SelectListItem>)ViewBag.SelectList)
                    {
                        <option value="@role.Value">@role.Text</option>
                    }
                </select>
            </div>


        </div>*@
</div>

@section Scripts{
    <partial name="_ValidationScriptsPartial.cshtml">
        @*<script>
            $(function () {

                $("form").on("submit", () => {
                    $("#Name").val("");
                })
            })

        </script>*@

}
